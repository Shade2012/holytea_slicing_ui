import 'package:flutter/material.dart';

Widget eWallet() {
  return Container(
    margin: EdgeInsets.symmetric(horizontal : 10),
    color: Colors.black,
    child: Row(
      children: [
        Container(
          //saldo
        ),
        Column(
          children: [
            Image.asset("name"),
            //pay
          ],
        ),
        Column(
          children: [
            Image.asset("name",
            )
            //top up
          ],
        )
      ],
    ),
  );
}
